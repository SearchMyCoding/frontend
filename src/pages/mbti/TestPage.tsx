import { MbitTestTemplate } from "src/components";

export function TestPage(): JSX.Element {
  const title: string = "Mbti Test Page";
  return (
    <div title={title}>
      <MbitTestTemplate />
    </div>
  );
};
